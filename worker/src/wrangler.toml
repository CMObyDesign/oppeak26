name = "swot-engine"
main = "worker/src/index.js"
compatibility_date = "2024-01-01"

[vars]
# Non-sensitive config — safe to commit
GHL_LOCATION_ID = "oLIENQCtGnt9U6gfLhE5"

# Secrets — set via dashboard or wrangler CLI, never commit
# Run these commands after deployment:
#   wrangler secret put ANTHROPIC_API_KEY
#   wrangler secret put GHL_API_KEY

[[routes]]
# Update this after deployment with your actual worker URL
# pattern = "swot-engine.YOUR_SUBDOMAIN.workers.dev/*"
